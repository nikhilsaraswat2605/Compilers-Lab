int main()
{
    int a, b, c, d;
    // expamples of boolean expressions
    // examples of arithmetic expressions
    a = a & b;
    b = b | c;
    c = c & d;
    d = d | a;
    a = a << b;
    b = b >> c;
    c = c + (d * a) / b;
    d = d - (a * b) / c;

    float x, y, z;
    x = x + y;
    y = y - z;
    z = z * x;
    x = x / y + z;

    return 0;
}
